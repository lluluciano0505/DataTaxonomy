# Streamlit Cloud secrets (example only, do not commit real secrets)
# To use: copy this to .streamlit/secrets.toml and add your real values

[openai]
api_key = "your-openai-api-key-here"

[anthropic] 
api_key = "your-anthropic-api-key-here"

# For local development, you can also use .env file
# The app will work without these for demo purposes